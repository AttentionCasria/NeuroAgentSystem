<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.it.mapper.RegiMapper">

    <insert id="insertUser" parameterType="com.it.pojo.User" useGeneratedKeys="true" keyProperty="id">
        insert into user (name, password, image, create_time, update_time)
        values (#{name}, #{password}, #{image}, #{createTime}, #{updateTime})
    </insert>
</mapper>